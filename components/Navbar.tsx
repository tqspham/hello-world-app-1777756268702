"use client";

export default function Navbar() {
  // This component is now replaced by Sidebar functionality
  // Kept as empty/minimal to avoid breaking imports if needed elsewhere
  return null;
}
